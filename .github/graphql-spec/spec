type Query {
  node(id: ID!): Node
  authPayload(id: ID!): AuthPayload
  authPayloads(
    "Returns the first n elements from the list."
    first: Int

    "Returns the last n elements from the list."
    last: Int

    "Returns the elements in the list that come before the specified cursor."
    before: String

    "Returns the elements in the list that come after the specified cursor."
    after: String
  ): AuthPayloadCursorConnection
  emptyResponse(id: ID!): EmptyResponse
  emptyResponses(
    "Returns the first n elements from the list."
    first: Int

    "Returns the last n elements from the list."
    last: Int

    "Returns the elements in the list that come before the specified cursor."
    before: String

    "Returns the elements in the list that come after the specified cursor."
    after: String
  ): EmptyResponseCursorConnection
  healthCheck(id: ID!): HealthCheck
  healthChecks(
    "Returns the first n elements from the list."
    first: Int

    "Returns the last n elements from the list."
    last: Int

    "Returns the elements in the list that come before the specified cursor."
    before: String

    "Returns the elements in the list that come after the specified cursor."
    after: String
  ): HealthCheckCursorConnection
  user(id: ID!): User
  users(
    "Returns the first n elements from the list."
    first: Int

    "Returns the last n elements from the list."
    last: Int

    "Returns the elements in the list that come before the specified cursor."
    before: String

    "Returns the elements in the list that come after the specified cursor."
    after: String
  ): UserCursorConnection
}

"A node, according to the Relay specification."
interface Node {
  "The id of this node."
  id: ID!
}

type AuthPayload implements Node {
  id: ID!
}

"Cursor connection for AuthPayload."
type AuthPayloadCursorConnection {
  edges: [AuthPayloadEdge]
  pageInfo: AuthPayloadPageInfo!
  totalCount: Int!
}

"Edge of AuthPayload."
type AuthPayloadEdge {
  node: AuthPayload
  cursor: String!
}

"Information about the current page."
type AuthPayloadPageInfo {
  endCursor: String
  startCursor: String
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
}

type EmptyResponse implements Node {
  id: ID!
}

"Cursor connection for EmptyResponse."
type EmptyResponseCursorConnection {
  edges: [EmptyResponseEdge]
  pageInfo: EmptyResponsePageInfo!
  totalCount: Int!
}

"Edge of EmptyResponse."
type EmptyResponseEdge {
  node: EmptyResponse
  cursor: String!
}

"Information about the current page."
type EmptyResponsePageInfo {
  endCursor: String
  startCursor: String
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
}

type HealthCheck implements Node {
  id: ID!
}

"Cursor connection for HealthCheck."
type HealthCheckCursorConnection {
  edges: [HealthCheckEdge]
  pageInfo: HealthCheckPageInfo!
  totalCount: Int!
}

"Edge of HealthCheck."
type HealthCheckEdge {
  node: HealthCheck
  cursor: String!
}

"Information about the current page."
type HealthCheckPageInfo {
  endCursor: String
  startCursor: String
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
}

type User implements Node {
  id: ID!
  confirmed: Boolean!
  email: String!
  initials: String!
}

"Cursor connection for User."
type UserCursorConnection {
  edges: [UserEdge]
  pageInfo: UserPageInfo!
  totalCount: Int!
}

"Edge of User."
type UserEdge {
  node: User
  cursor: String!
}

"Information about the current page."
type UserPageInfo {
  endCursor: String
  startCursor: String
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
}

type Mutation {
  "SignIns a AuthPayload."
  signInUser(input: signInUserInput!): signInUserPayload

  "CompleteTwoFactors a AuthPayload."
  completeTwoFactorUser(input: completeTwoFactorUserInput!): completeTwoFactorUserPayload

  "RefreshTokens a AuthPayload."
  refreshTokenUser(input: refreshTokenUserInput!): refreshTokenUserPayload

  "SetupTwoFactors a AuthPayload."
  setupTwoFactorUser(input: setupTwoFactorUserInput!): setupTwoFactorUserPayload

  "ConfirmTwoFactors a AuthPayload."
  confirmTwoFactorUser(input: confirmTwoFactorUserInput!): confirmTwoFactorUserPayload

  "DisableTwoFactors a AuthPayload."
  disableTwoFactorUser(input: disableTwoFactorUserInput!): disableTwoFactorUserPayload

  "RegenerateRecoveryCodess a AuthPayload."
  regenerateRecoveryCodesUser(input: regenerateRecoveryCodesUserInput!): regenerateRecoveryCodesUserPayload

  "SignOuts a AuthPayload."
  signOutUser(input: signOutUserInput!): signOutUserPayload

  "SignOutAlls a AuthPayload."
  signOutAllUser(input: signOutAllUserInput!): signOutAllUserPayload

  "Updates a EmptyResponse."
  updateEmptyResponse(input: updateEmptyResponseInput!): updateEmptyResponsePayload

  "Deletes a EmptyResponse."
  deleteEmptyResponse(input: deleteEmptyResponseInput!): deleteEmptyResponsePayload

  "Creates a EmptyResponse."
  createEmptyResponse(input: createEmptyResponseInput!): createEmptyResponsePayload

  "Updates a HealthCheck."
  updateHealthCheck(input: updateHealthCheckInput!): updateHealthCheckPayload

  "Deletes a HealthCheck."
  deleteHealthCheck(input: deleteHealthCheckInput!): deleteHealthCheckPayload

  "Creates a HealthCheck."
  createHealthCheck(input: createHealthCheckInput!): createHealthCheckPayload

  "Deletes a User."
  deleteUser(input: deleteUserInput!): deleteUserPayload

  "Confirms a User."
  confirmUser(input: confirmUserInput!): confirmUserPayload

  "Creates a User."
  createUser(input: createUserInput!): createUserPayload

  "Updates a User."
  updateUser(input: updateUserInput!): updateUserPayload

  "ResendEmailTos a User."
  resendEmailToUser(input: resendEmailToUserInput!): resendEmailToUserPayload

  "RequestPasswordResets a User."
  requestPasswordResetUser(input: requestPasswordResetUserInput!): requestPasswordResetUserPayload

  "ConfirmPasswordResets a User."
  confirmPasswordResetUser(input: confirmPasswordResetUserInput!): confirmPasswordResetUserPayload
}

"SignIns a AuthPayload."
input signInUserInput {
  clientMutationId: String
  email: String!
  password: String!
  rememberMe: Boolean
}

"SignIns a AuthPayload."
type signInUserPayload {
  user: signInUserPayloadData
  clientMutationId: String
}

"SignIns a AuthPayload."
type signInUserPayloadData implements Node {
  id: ID!
  _id: String!
  success: Boolean!
  twoFactorEnabled: Boolean
  accessToken: String
  refreshToken: String
  pendingSessionId: String
  otpauthUri: String
  secret: String
  recoveryCodes: Iterable!
  recoveryCodesRemaining: Int
  warning: String
}

"The `Iterable` scalar type represents an array or a Traversable with any kind of data."
scalar Iterable

"CompleteTwoFactors a AuthPayload."
input completeTwoFactorUserInput {
  clientMutationId: String
  pendingSessionId: String!
  twoFactorCode: String!
}

"CompleteTwoFactors a AuthPayload."
type completeTwoFactorUserPayload {
  user: completeTwoFactorUserPayloadData
  clientMutationId: String
}

"CompleteTwoFactors a AuthPayload."
type completeTwoFactorUserPayloadData implements Node {
  id: ID!
  _id: String!
  success: Boolean!
  twoFactorEnabled: Boolean
  accessToken: String
  refreshToken: String
  pendingSessionId: String
  otpauthUri: String
  secret: String
  recoveryCodes: Iterable!
  recoveryCodesRemaining: Int
  warning: String
}

"RefreshTokens a AuthPayload."
input refreshTokenUserInput {
  clientMutationId: String
  refreshToken: String!
}

"RefreshTokens a AuthPayload."
type refreshTokenUserPayload {
  user: refreshTokenUserPayloadData
  clientMutationId: String
}

"RefreshTokens a AuthPayload."
type refreshTokenUserPayloadData implements Node {
  id: ID!
  _id: String!
  success: Boolean!
  twoFactorEnabled: Boolean
  accessToken: String
  refreshToken: String
  pendingSessionId: String
  otpauthUri: String
  secret: String
  recoveryCodes: Iterable!
  recoveryCodesRemaining: Int
  warning: String
}

"SetupTwoFactors a AuthPayload."
input setupTwoFactorUserInput {
  clientMutationId: String
}

"SetupTwoFactors a AuthPayload."
type setupTwoFactorUserPayload {
  user: setupTwoFactorUserPayloadData
  clientMutationId: String
}

"SetupTwoFactors a AuthPayload."
type setupTwoFactorUserPayloadData implements Node {
  id: ID!
  _id: String!
  success: Boolean!
  twoFactorEnabled: Boolean
  accessToken: String
  refreshToken: String
  pendingSessionId: String
  otpauthUri: String
  secret: String
  recoveryCodes: Iterable!
  recoveryCodesRemaining: Int
  warning: String
}

"ConfirmTwoFactors a AuthPayload."
input confirmTwoFactorUserInput {
  clientMutationId: String
  twoFactorCode: String!
}

"ConfirmTwoFactors a AuthPayload."
type confirmTwoFactorUserPayload {
  user: confirmTwoFactorUserPayloadData
  clientMutationId: String
}

"ConfirmTwoFactors a AuthPayload."
type confirmTwoFactorUserPayloadData implements Node {
  id: ID!
  _id: String!
  success: Boolean!
  twoFactorEnabled: Boolean
  accessToken: String
  refreshToken: String
  pendingSessionId: String
  otpauthUri: String
  secret: String
  recoveryCodes: Iterable!
  recoveryCodesRemaining: Int
  warning: String
}

"DisableTwoFactors a AuthPayload."
input disableTwoFactorUserInput {
  clientMutationId: String
  twoFactorCode: String!
}

"DisableTwoFactors a AuthPayload."
type disableTwoFactorUserPayload {
  user: disableTwoFactorUserPayloadData
  clientMutationId: String
}

"DisableTwoFactors a AuthPayload."
type disableTwoFactorUserPayloadData implements Node {
  id: ID!
  _id: String!
  success: Boolean!
  twoFactorEnabled: Boolean
  accessToken: String
  refreshToken: String
  pendingSessionId: String
  otpauthUri: String
  secret: String
  recoveryCodes: Iterable!
  recoveryCodesRemaining: Int
  warning: String
}

"RegenerateRecoveryCodess a AuthPayload."
input regenerateRecoveryCodesUserInput {
  clientMutationId: String
}

"RegenerateRecoveryCodess a AuthPayload."
type regenerateRecoveryCodesUserPayload {
  user: regenerateRecoveryCodesUserPayloadData
  clientMutationId: String
}

"RegenerateRecoveryCodess a AuthPayload."
type regenerateRecoveryCodesUserPayloadData implements Node {
  id: ID!
  _id: String!
  success: Boolean!
  twoFactorEnabled: Boolean
  accessToken: String
  refreshToken: String
  pendingSessionId: String
  otpauthUri: String
  secret: String
  recoveryCodes: Iterable!
  recoveryCodesRemaining: Int
  warning: String
}

"SignOuts a AuthPayload."
input signOutUserInput {
  clientMutationId: String
}

"SignOuts a AuthPayload."
type signOutUserPayload {
  user: signOutUserPayloadData
  clientMutationId: String
}

"SignOuts a AuthPayload."
type signOutUserPayloadData implements Node {
  id: ID!
  _id: String!
  success: Boolean!
  twoFactorEnabled: Boolean
  accessToken: String
  refreshToken: String
  pendingSessionId: String
  otpauthUri: String
  secret: String
  recoveryCodes: Iterable!
  recoveryCodesRemaining: Int
  warning: String
}

"SignOutAlls a AuthPayload."
input signOutAllUserInput {
  clientMutationId: String
}

"SignOutAlls a AuthPayload."
type signOutAllUserPayload {
  user: signOutAllUserPayloadData
  clientMutationId: String
}

"SignOutAlls a AuthPayload."
type signOutAllUserPayloadData implements Node {
  id: ID!
  _id: String!
  success: Boolean!
  twoFactorEnabled: Boolean
  accessToken: String
  refreshToken: String
  pendingSessionId: String
  otpauthUri: String
  secret: String
  recoveryCodes: Iterable!
  recoveryCodesRemaining: Int
  warning: String
}

"Updates a EmptyResponse."
input updateEmptyResponseInput {
  id: ID!
  clientMutationId: String
}

"Updates a EmptyResponse."
type updateEmptyResponsePayload {
  emptyResponse: EmptyResponse
  clientMutationId: String
}

"Deletes a EmptyResponse."
input deleteEmptyResponseInput {
  id: ID!
  clientMutationId: String
}

"Deletes a EmptyResponse."
type deleteEmptyResponsePayload {
  emptyResponse: EmptyResponse
  clientMutationId: String
}

"Creates a EmptyResponse."
input createEmptyResponseInput {
  clientMutationId: String
}

"Creates a EmptyResponse."
type createEmptyResponsePayload {
  emptyResponse: EmptyResponse
  clientMutationId: String
}

"Updates a HealthCheck."
input updateHealthCheckInput {
  id: ID!
  clientMutationId: String
}

"Updates a HealthCheck."
type updateHealthCheckPayload {
  healthCheck: HealthCheck
  clientMutationId: String
}

"Deletes a HealthCheck."
input deleteHealthCheckInput {
  id: ID!
  clientMutationId: String
}

"Deletes a HealthCheck."
type deleteHealthCheckPayload {
  healthCheck: HealthCheck
  clientMutationId: String
}

"Creates a HealthCheck."
input createHealthCheckInput {
  clientMutationId: String
}

"Creates a HealthCheck."
type createHealthCheckPayload {
  healthCheck: HealthCheck
  clientMutationId: String
}

"Deletes a User."
input deleteUserInput {
  id: ID!
  clientMutationId: String
}

"Deletes a User."
type deleteUserPayload {
  user: deleteUserPayloadData
  clientMutationId: String
}

"Deletes a User."
type deleteUserPayloadData implements Node {
  id: ID!
}

"Confirms a User."
input confirmUserInput {
  clientMutationId: String
  token: String!
}

"Confirms a User."
type confirmUserPayload {
  user: User
  clientMutationId: String
}

"Creates a User."
input createUserInput {
  clientMutationId: String
  email: String!
  initials: String!
  password: String!
}

"Creates a User."
type createUserPayload {
  user: User
  clientMutationId: String
}

"Updates a User."
input updateUserInput {
  clientMutationId: String
  id: ID!
  newPassword: String
  password: String!
  email: String
  initials: String
}

"Updates a User."
type updateUserPayload {
  user: User
  clientMutationId: String
}

"ResendEmailTos a User."
input resendEmailToUserInput {
  clientMutationId: String
  id: ID!
}

"ResendEmailTos a User."
type resendEmailToUserPayload {
  user: User
  clientMutationId: String
}

"RequestPasswordResets a User."
input requestPasswordResetUserInput {
  clientMutationId: String
  email: String!
}

"RequestPasswordResets a User."
type requestPasswordResetUserPayload {
  user: User
  clientMutationId: String
}

"ConfirmPasswordResets a User."
input confirmPasswordResetUserInput {
  clientMutationId: String
  token: String!
  newPassword: String!
}

"ConfirmPasswordResets a User."
type confirmPasswordResetUserPayload {
  user: User
  clientMutationId: String
}
