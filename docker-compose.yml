version: '3.8'

services:
  structurizr:
    image: structurizr/lite
    ports:
      - '8080:8080'
    volumes:
      - ./:/usr/local/structurizr
  nodejs:
    build: .
    restart: unless-stopped
    volumes:
      - .:/app
    ports:
      - '3000:3000'
