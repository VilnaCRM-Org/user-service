<doctrine-mongo-mapping
  xmlns="http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping
                                            http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping.xsd"
>

    <document name="App\OAuth\Domain\Entity\SocialIdentity" db="user_service" collection="social_identities">

        <id field-name="id" type="string" strategy="NONE" />

        <field field-name="provider" type="string" />
        <field field-name="providerId" name="provider_id" type="string" />
        <field field-name="userId" name="user_id" type="string" />
        <field field-name="createdAt" name="created_at" type="date_immutable" />
        <field field-name="lastUsedAt" name="last_used_at" type="date_immutable" />

        <indexes>
            <!-- Unique: one social identity per provider account -->
            <index unique="true">
                <key name="provider" />
                <key name="provider_id" />
            </index>

            <!-- Unique: one link per provider per user -->
            <index unique="true">
                <key name="user_id" />
                <key name="provider" />
            </index>

            <!-- Lookup by user -->
            <index>
                <key name="user_id" />
            </index>
        </indexes>
    </document>
</doctrine-mongo-mapping>
