<doctrine-mongo-mapping
  xmlns="http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping
                                            http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping.xsd"
>

    <document name="App\OAuth\Domain\Entity\AccessTokenDocument" db="user_service" collection="oauth2_access_tokens">

        <id field-name="identifier" type="string" strategy="NONE" />

        <field field-name="expiry" type="date" />
        <field field-name="userIdentifier" type="string" nullable="true" />
        <field field-name="clientIdentifier" type="string" />
        <field field-name="revoked" type="boolean" />
        <field field-name="scopes" type="collection" />

        <indexes>
            <!-- Index for expiry queries (cleanup) -->
            <index>
                <key name="expiry" order="asc" />
            </index>

            <!-- Index for user identifier queries -->
            <index>
                <key name="userIdentifier" order="asc" />
            </index>

            <!-- Index for client identifier queries -->
            <index>
                <key name="clientIdentifier" order="asc" />
            </index>
        </indexes>
    </document>
</doctrine-mongo-mapping>
